package dev.sch39.template;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.HashMap;
import java.util.Map;

import org.junit.jupiter.api.Test;

public class BladeTemplateEngineTest {
  // private BladeTemplateEngine templateEngine = new BladeTemplateEngine();

  @Test
  public void testRenderTemplate() throws Exception {
    // BladeTemplateEngine templateEngine = new BladeTemplateEngine();
    // String templatePath = "src/test/resources/templates/testTemplate.blade";
    // String content = "<h1>Hello, {{name}} ! {{ name }}</h1>";
    // Files.createDirectories(Paths.get("src/test/resources/templates"));
    // Files.writeString(Path.of(templatePath), content);

    // Map<String, Object> model = new HashMap<>();
    // model.put("name", "World");

    // String renderedTemplate = templateEngine.renderTemplate(templatePath, model);

    // assertEquals("<h1>Hello, World ! World</h1>", renderedTemplate);

    // Files.deleteIfExists(Paths.get(templatePath));
  }
}
